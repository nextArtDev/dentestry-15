generator client {
  provider = "prisma-client-js"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

enum Role {
  user
  admin
  doctor
}

model User {
  id   String @id
  name String @db.Text
  role Role   @default(user)
}
